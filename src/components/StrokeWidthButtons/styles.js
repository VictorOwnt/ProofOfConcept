import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  functionButton: {
    marginHorizontal: 5,
    marginVertical: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
